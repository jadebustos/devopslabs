# First steps with containers

You will need a virtual machine (CentOS Stream 9). You can deploy it with a minimal installation and update it to last level:

```console
[root@podman ~]# dnf update -y
```

[Deploy ansible](../wrkshp-ansible/00-1st-steps.md) and create a non-root user with passworless permissions.

